# Redis-Study-Space
个人Redis学习空间

## 快速启动（基于docker）
- 启动`Docker Desktop`
- 输入命令：
  ```powershell
  docker start redis
  ```
- 启动`Redis CLI`会话框：
  ```powershell
  docker exec -it redis redis-cli
  ```
- 退出`cli`会话：
  `Ctrl+C`或者输入`exit`即可退出。
  
